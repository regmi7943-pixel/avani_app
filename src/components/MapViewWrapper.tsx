import MapView, { Marker, Polygon, Polyline, Polygon as MapPolygon, PROVIDER_DEFAULT } from 'react-native-maps';

export { MapView, Marker, Polygon, Polyline, MapPolygon, PROVIDER_DEFAULT };
export default MapView;
